var numeri = [];

var sum = 0;

for ( var i = 0; i < 5; i++) {
    var numeroUtente = parseInt(prompt(" Inserisci un numero "));

    numeri.push(numeroUtente);

    var somma += numeri[i];

    console.log(somma)

}

// var sommaNumeri = numeroUtente

// document.getElementById("somma").innerHTML = "somma" += numeroUtente ;
